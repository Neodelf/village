= render 'shared/navpanel'

= render 'main'
= render 'work'
= render 'about'
= render 'portfolio'
= render 'genplan'
= render 'map'
= render 'contacts'
//= render 'footer'

:javascript
  $(document).ready(function() {
    $('#main').backstretch([
       "#{image_url('bg_1.jpg')}",
       "#{image_url('bg_2.jpg')}"
        ],  {duration: 2000, fade: 750}
    );
  });